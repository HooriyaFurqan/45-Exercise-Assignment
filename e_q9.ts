/* Store your favouite number in a variable. Then, using that variable, create a message that reveals your favourite number. Print that message.*/
const favNum: number = 7;
const message: string = ` My favourite Number is "${favNum}"`;
console.log(message);
